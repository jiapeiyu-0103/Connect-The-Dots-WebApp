import PropTypes from 'prop-types';
function MyDriftBottle() {

            return (
                <div> This is the MyDriftBottle page </div>
            );
 
 
}

MyDriftBottle.propTypes = {
    closeModal:PropTypes.func,
}

export default MyDriftBottle;
